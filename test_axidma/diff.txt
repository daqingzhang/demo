diff --git a/drivers/dma/rda_axidma.c b/drivers/dma/rda_axidma.c
index c41790edcae4..4a81409b14d2 100644
--- a/drivers/dma/rda_axidma.c
+++ b/drivers/dma/rda_axidma.c
@@ -451,12 +451,10 @@ static int rda_dma_config_transfer(struct rda_dma_chan *c, struct rda_dma_sg *ds
 {
 	unsigned int pcount;
 
-	if(dsg->len < 256)
-		pcount = 1;
-	else if(dsg->len < 65535)
-		pcount = 32;
+	if(dsg->len < 0xffff)
+		pcount = 1000;
 	else
-		pcount = 64;
+		pcount = 4096;
 
 	dma_debug("%s, dsg(%p), src=%x, dst=%x, len=%d\n", __func__,
 			dsg, dsg->src_addr, dsg->dst_addr, dsg->len);
@@ -556,6 +554,7 @@ static enum dma_status rda_dma_tx_status(struct dma_chan *chan,
 	return ret;
 }
 
+#if 0
 static int rda_dma_hard_sync(struct rda_dma_chan *c, unsigned int tm)
 {
 	unsigned int status, bytes;
@@ -578,6 +577,22 @@ static int rda_dma_hard_sync(struct rda_dma_chan *c, unsigned int tm)
 	dma_debug("%s, done\n",__func__);
 	return 0;
 }
+#else
+static int rda_dma_hard_sync(struct rda_dma_chan *c, unsigned int tm)
+{
+	unsigned int status, bytes;
+
+	do {
+		udelay(5);
+		bytes = axidma_ch_get_count(c);
+		status = axidma_ch_get_status(c) & ARM_AXIDMA_RUN;
+	} while (status || bytes);
+
+	c->state = RDA_DMA_CH_IDLE;
+	dma_debug("%s, done\n",__func__);
+	return 0;
+}
+#endif
 
 static void rda_dma_del_handler(struct rda_dma_chan *c)
 {
